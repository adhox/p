package main

// func (p Panel) Pivot(col string, fn interface{}) Panel {
//   // example: pnl.Pivot("ages", panel.Sum)

//   // find unique values for p[col]
//   groupBy := NewPanel()
//   groupBy.AddSeries(col, []interface{}{})
//   // insert unique values...

//     for _, unique:= range groupBy[col] {
//        // apply fn for each unique value in p[col] for each subsequent column
//        // might have to do switch statement for true comparison
//        a:= p.Filter(col, func(v interface{}) bool { return unique == v })

//         fn(a)

//     }

//   // return aggregate panel

// }
